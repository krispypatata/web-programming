import { useState } from "react";

export default function useCart() {

    // Create hooks for cart total
    // Create hooks for cart (items)

    const handleSubmit = (item) => {
        // Output added to cart
        // SetCart
        // Set Cart Total
    }
    
    return {
        // return the react hooks and functions
    }
}